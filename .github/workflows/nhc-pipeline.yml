name: NHC Pipeline (IEM + Claude Rewrite)

on:
  # DISABLED (superseded by the continuous fast-watch version): schedule:
  # - cron: "5 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run pipeline
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          ALERT_TO: ${{ secrets.ALERT_TO }}
        run: python nhc_pipeline.py

      - name: Commit updated state
        run: |
          git config user.name "nhc-pipeline-bot"
          git config user.email "actions@users.noreply.github.com"
          git add pipeline_state.json
          git diff --quiet --cached || git commit -m "Update pipeline state [skip ci]"
          git pull --rebase --autostash || true
          git push
